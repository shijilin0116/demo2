import React, {useContext} from 'react';
import DeleteNodeFormContext from "../context/DeleteNodeFormContext";

const UseDeleteNodeFormContext = () => {
    return useContext(DeleteNodeFormContext)
};

export default UseDeleteNodeFormContext;
